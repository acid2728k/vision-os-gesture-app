# 🔑 Как получить GitHub Personal Access Token

## 📋 Пошаговая инструкция

### Шаг 1: Перейдите на страницу токенов
1. Откройте https://github.com/settings/tokens
2. Или: GitHub → ваш аватар (правый верхний угол) → **Settings** → в левом меню **Developer settings** → **Personal access tokens** → **Tokens (classic)**

### Шаг 2: Создайте новый токен
1. Нажмите кнопку **"Generate new token"**
2. Выберите **"Generate new token (classic)"** (не fine-grained)

### Шаг 3: Настройте токен
1. **Note** (название): `VISION_OS Setup` (или любое другое понятное название)
2. **Expiration** (срок действия): 
   - Рекомендуется: **90 days** (можно продлить позже)
   - Или: **No expiration** (если хотите, чтобы токен работал всегда)

### Шаг 4: Выберите права (scopes)
**ВАЖНО:** Отметьте галочку **`repo`** (полный доступ к репозиториям)

Это даст права на:
- ✅ Создание репозиториев
- ✅ Чтение и запись кода
- ✅ Управление Issues и Pull Requests
- ✅ Управление настройками репозитория

**Другие права НЕ нужны** для создания репозитория.

### Шаг 5: Создайте токен
1. Прокрутите вниз
2. Нажмите **"Generate token"** (зеленая кнопка)

### Шаг 6: Скопируйте токен
⚠️ **ВАЖНО:** Токен показывается только один раз!

1. **Сразу скопируйте токен** (выглядит как: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`)
2. Сохраните его в безопасном месте (например, в менеджере паролей)
3. Если потеряете - придется создать новый

---

## 🚀 Использование токена

### Вариант 1: Через скрипт (рекомендуется)

**Для Mac/Linux:**
```bash
cd vision-os-gesture-app
chmod +x create-repo-with-api.sh
./create-repo-with-api.sh YOUR_TOKEN YOUR_USERNAME
```

**Для Windows (PowerShell):**
```powershell
cd vision-os-gesture-app
.\create-repo-with-api.ps1 -GitHubToken "YOUR_TOKEN" -GitHubUsername "YOUR_USERNAME"
```

### Вариант 2: Вручную через команды

```bash
# Создание репозитория через API
curl -X POST \
  -H "Authorization: token YOUR_TOKEN" \
  -H "Accept: application/vnd.github.v3+json" \
  https://api.github.com/user/repos \
  -d '{"name":"vision-os-gesture-app","description":"VISION_OS v0.9.1","private":false}'
```

---

## 🔒 Безопасность

### ✅ Что делать:
- ✅ Храните токен в безопасном месте
- ✅ Используйте токен только локально (не коммитьте в git!)
- ✅ Удаляйте неиспользуемые токены
- ✅ Устанавливайте срок действия токена

### ❌ Чего НЕ делать:
- ❌ НЕ коммитьте токен в git
- ❌ НЕ отправляйте токен в сообщениях/чатах
- ❌ НЕ используйте один токен для всех проектов
- ❌ НЕ давайте токен другим людям

---

## 🆘 Проблемы и решения

### "Bad credentials" ошибка
- Проверьте, что скопировали токен полностью
- Убедитесь, что токен не истек
- Проверьте, что выбрали scope `repo`

### "Token has expired"
- Создайте новый токен
- Или продлите существующий (если возможно)

### "Repository already exists"
- Репозиторий с таким именем уже существует
- Используйте другое имя или удалите старый репозиторий

---

## 📝 Пример токена

Токен выглядит примерно так:
```
ghp_1234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234
```

Начинается с `ghp_` и содержит около 40 символов.

---

## ✅ Готово!

После получения токена запустите скрипт, и репозиторий будет создан автоматически!

**Вопросы?** Создайте Issue в репозитории или спросите в Discussions.

